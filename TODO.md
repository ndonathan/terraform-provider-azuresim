# TODO

> Resource backlog lives in `BACKLOG.md`. This file is for non-feature work.

- [x] ~~GitHub PR-flow consideration.~~ **Resolved 2026-05-03 — strict branch+PR flow going forward.** Reason: the project has external users (GitHub-published terraform provider), so direct pushes to `main` (even via admin override) violate the contract the branch-protection rule signals. Going forward, every change goes via a topic branch + PR. The 2026-05-01 admin-override push (`d56cd25`) was a one-time WIP-rescue exception and should not be repeated.
- [x] ~~README "Supported Resources" table is stale.~~ Done 2026-05-03 — replaced 13-row flat table with a 5-section categorized listing of all 50 resources (Foundational / Networking / Compute / Data & messaging / Observability & security), each with its `azurerm_*` equivalent.
- [x] ~~**README "Resource Reference" section is stale.**~~ Done 2026-05-18 — chose auto-generation over hand-curated pointer. Added `scripts/generate-resource-table.py` (stdlib-only Python, idempotent, supports `--write` / `--check`); replaced both the stale "Supported Resources" (`azurerm_*` mapping, 8 of 61) and "Resource Reference" (per-attribute tables, 8 of 61) sections with a single auto-generated grouped table between `<!-- AUTO-GENERATED RESOURCES START/END -->` markers — 61 resources across 15 subcategories, each row linking to its `docs/resources/*.md`. CI job `docs-table` in `.github/workflows/test.yml` runs `--check` on every push/PR so README drift fails CI. PROJECT.md Goal ticked.
- [x] ~~README "Project Structure" diagram is stale.~~ Done 2026-05-03 — diagram refreshed: collapses the per-resource enumeration to `resource_*.go (50 implementations)` + `*_test.go`, and surfaces the previously-omitted real artifacts (GNUmakefile, .goreleaser.yml, terraform-registry-manifest.json, CI workflows, docs/resources/).
- [x] ~~**`docs/resources/` coverage gap.**~~ Done 2026-05-07 — produced 48 new markdown files covering every resource type registered in `provider.go`, bringing `docs/resources/` to 61 files (one per registered resource type, not per Go source file). Discrepancy surfaced: dispatch assumed 50 resources / 37 missing docs (1:1 with Go files), but several Go files implement multiple resource types (e.g. `resource_servicebus_children.go` registers 3, `resource_web_app.go` and `resource_function_app.go` and `resource_vmss.go` each register 2, `resource_route_table.go` and `resource_private_dns_zone.go` each register 2, `resource_mssql.go` registers 2, `resource_postgresql_flexible.go` registers both Postgres and MySQL, `resource_container_app.go` registers 2, `resource_monitor_alerts.go` registers 2). Actual registered count is 61, not 50. PROJECT.md description ("Supports 50 resource types") and BACKLOG.md count are now stale.
- [x] ~~Built provider binary committed at repo root.~~ **FALSE POSITIVE — verified 2026-05-03.** Auditor's analysis of the gitignore pattern was wrong. `git check-ignore -v` confirms `terraform-provider-azuresim*` (line 2 of .gitignore) catches the binary file; the `!terraform-provider-azuresim/` negation only applies to directories with that exact name (trailing `/` matters), not files. Binary exists on disk (~25 MB) but is not tracked.
- [x] ~~PROJECT.md SDLC criteria don't mention tests or CI.~~ Done 2026-05-03 — added three new project-specific criteria (acceptance/unit tests, CI workflows, GNUmakefile) and ticked them.
- [x] ~~Verify `gpg-terraform-azuresim/` sibling exists.~~ Verified 2026-05-03 — sibling still at `/Users/ndonathan/Documents/projects/gpg-terraform-azuresim/` (status `maintenance`, intentionally local-only by design). Notes pointer is correct.
- [x] ~~**Resource count is stale.**~~ Done 2026-05-17 — verified ground truth by counting registrations in `internal/provider/provider.go` `Resources()` slice (61, matched by `grep -cE "^\s+New[A-Z].*Resource,?\s*$"`). Updated PROJECT.md description ("Supports 61 resource types") and Goals denominators ("13 of 61", "8 of 61"); updated README "Supported Resources" header ("61 resource types") and "Project Structure" diagram ("61 resource implementations", "13 of 61 — see TODO"). BACKLOG.md was scanned and contains no "50"/"fifty" count citation — no edit needed there.
- [ ] **Doc-coverage numerator is also stale.** PROJECT.md Goal ("13 of 61") and README Project-Structure note ("13 of 61 — see TODO") both still claim 13 docs exist. Verified 2026-05-17: `docs/resources/` contains 61 markdown files (one per registered type, per the 2026-05-07 doc-batch grind). The per-resource doc gap is closed. PM should review whether to tick the PROJECT.md Goal "Per-resource docs to 100% coverage" and update the README diagram caption.
