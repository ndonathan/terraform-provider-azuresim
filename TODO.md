# TODO

> Resource backlog lives in `BACKLOG.md`. This file is for non-feature work.

- [ ] **GitHub PR-flow consideration.** The `main` branch on `github.com/ndonathan/terraform-provider-azuresim` has a "Changes must be made through a pull request" branch protection rule. The 2026-05-01 WIP-rescue push (`d56cd25`) bypassed the rule via admin override. If you want strict PR flow going forward, future direct pushes to `main` should be replaced with branch + PR. Otherwise, no action — the rule is informational at the admin level.
- [ ] **README "Supported Resources" table is stale.** Lists only 13 resources; provider implements 50+ per BACKLOG.md and `internal/provider/resource_*.go` count. Update the table or replace with a generated/linked summary.
- [ ] **README "Resource Reference" section is stale.** Only documents 8 resources (`resource_group`, `virtual_network`, `subnet`, `virtual_machine`, `network_security_group`, `public_ip`, `network_interface`, `storage_account`). Missing per-attribute reference for the other 40+ resources, or remove the section in favor of `docs/resources/`.
- [ ] **README "Project Structure" diagram is stale.** Shows 8 resource files; actual `internal/provider/` has 60+ Go files (resources + tests). Refresh or simplify the diagram.
- [ ] **`docs/resources/` coverage gap.** Only 13 markdown files (`key_vault.md`, `log_analytics_workspace.md`, `managed_disk.md`, `network_interface.md`, `network_security_group.md`, `public_ip.md`, `resource_group.md`, `storage_account.md`, `storage_container.md`, `subnet.md`, `user_assigned_identity.md`, `virtual_machine.md`, `virtual_network.md`); missing docs for 40+ implemented resources. Required for Terraform Registry publication completeness.
- [ ] **Built provider binary committed at repo root.** `terraform-provider-azuresim` (~25 MB) sits alongside source. `.gitignore` pattern `terraform-provider-azuresim*` is negated by `!terraform-provider-azuresim/` (which is meant to keep the project folder name itself unignored), so the binary slips through. Consider rewriting the gitignore to ignore only the binary form (e.g. `/terraform-provider-azuresim` with no trailing wildcard, paired with explicit `!*.go` style scoping) and removing the tracked binary from git history.
- [ ] **PROJECT.md SDLC criteria don't mention tests or CI.** `.github/workflows/test.yml` + `release.yml` exist and `internal/provider/*_test.go` test suites are present, but neither is listed as a project-specific SDLC criterion. Consider adding "tests present" and "CI workflows" checkboxes so future audits track them.
- [ ] **PROJECT.md Notes references `gpg-terraform-azuresim/` for signing keys.** Verify that sibling project still exists at `/Users/ndonathan/Documents/projects/gpg-terraform-azuresim/` and is reachable from the release workflow; if renamed or moved, update the Notes pointer.
